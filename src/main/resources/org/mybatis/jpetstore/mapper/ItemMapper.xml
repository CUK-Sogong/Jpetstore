<?xml version="1.0" encoding="UTF-8"?>
<!--

       Copyright 2010-2020 the original author or authors.

       Licensed under the Apache License, Version 2.0 (the "License");
       you may not use this file except in compliance with the License.
       You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

       Unless required by applicable law or agreed to in writing, software
       distributed under the License is distributed on an "AS IS" BASIS,
       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
       See the License for the specific language governing permissions and
       limitations under the License.

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mybatis.jpetstore.mapper.ItemMapper">

  <cache />

  <select id="getItemListByProduct" resultType="Item">
    SELECT
      I.ITEMID,
      LISTPRICE,
      UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      ATTR5 AS attribute5
    FROM ITEM I, PRODUCT P
    WHERE P.PRODUCTID = I.PRODUCTID
    AND I.PRODUCTID = #{value}
  </select>

  <select id="getItem" resultType="Item">
    select
      I.ITEMID,
      LISTPRICE,
      UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      ATTR5 AS attribute5,
      QTY AS quantity
    from ITEM I, INVENTORY V, PRODUCT P
    where P.PRODUCTID = I.PRODUCTID
      and I.ITEMID = V.ITEMID
      and I.ITEMID = #{value}
  </select>

  <select id="getInventoryQuantity" resultType="int">
    SELECT QTY AS value
    FROM INVENTORY
    WHERE ITEMID = #{itemId}
  </select>

  <update id="updateInventoryQuantity">
    UPDATE INVENTORY SET
      QTY = QTY - #{increment}
    WHERE ITEMID = #{itemId}
  </update>

  <select id="getUserItemList" resultType="Item">
    SELECT
      I.ITEMID,
      I.USERID,
      I.PRODUCTID,
      I.LISTPRICE,
      I.UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      I.date,
      I.CHARGE,
      I.SALE_STATUS AS saleStatus
    from ITEM I,  PRODUCT P
    where P.PRODUCTID = I.PRODUCTID
      and itemid LIKE '%SAL%'
  </select>

  <select id="getUserItemListByFilter" resultType="Item">
    SELECT
      I.ITEMID,
      I.USERID,
      I.PRODUCTID,
      I.LISTPRICE,
      I.UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      I.date,
      I.CHARGE,
      I.SALE_STATUS AS saleStatus
    from ITEM I,  PRODUCT P
    where P.PRODUCTID = I.PRODUCTID
      and I.itemid LIKE '%SAL%'
      and P.CATEGORY LIKE #{param1}
      and I.CHARGE LIKE #{param2}
      and P.NAME LIKE #{param4}
    ORDER BY
      CASE WHEN #{param3} = 0 THEN I.DATE END ASC,
      CASE WHEN #{param3} = 1 THEN I.DATE END DESC
  </select>

  <insert id="insertUserItem" parameterType="Item">
    INSERT INTO item (itemid, userid, productid, listprice, charge, date, sale_status, attr1, attr2, attr3, attr4)
    VALUES(#{itemId},#{userId},#{productId},#{listPrice},#{charge},NOW(),1,#{attribute1},#{attribute2},#{attribute3},#{attribute4});
  </insert>

  <select id="getUserItem" resultType="Item">
    SELECT
      I.ITEMID,
      I.USERID,
      I.PRODUCTID,
      I.LISTPRICE,
      I.UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      I.date,
      I.CHARGE,
      I.SALE_STATUS AS saleStatus
    from ITEM I,  PRODUCT P
    where P.PRODUCTID = I.PRODUCTID
      and itemid = #{itemId}
  </select>

  <update id="updateUserItem" parameterType="Item">

  </update>

</mapper>
